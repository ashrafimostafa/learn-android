<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_task_search"
        android:icon="@drawable/ic_search"
        android:title="search"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="always|collapseActionView" />

    <item
        android:icon="@drawable/ic_sort"
        android:title="sort"
        app:showAsAction="always">
        <menu>
            <item
                android:id="@+id/menu_task_sort_name"
                android:title="Sort by name" />
            <item
                android:id="@+id/menu_task_sort_date"
                android:title="Sort by creation date" />
        </menu>
    </item>

    <item
        android:id="@+id/menu_task_hide_complete_task"
        android:checkable="true"
        android:title="Hide completed"
        app:showAsAction="never"/>

    <item
        android:id="@+id/menu_task_delete_all_completed_task"
        android:title="Delete completed tasks"
        app:showAsAction="never"/>

</menu>